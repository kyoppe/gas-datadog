# clasp credentials (secret) - never commit
.clasprc.json
# NOTE: .clasp.json is committed on purpose (contains only scriptId, no secrets)

# local / secrets
*.local
*.local.*
.env
.env.*

# OS
.DS_Store

# node (if added later)
node_modules/
